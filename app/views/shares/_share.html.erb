<div class="container">
  <%= share.questions %> <%= link_to 'Edit', edit_week_share_path(share.week, share) %> | <%= link_to 'Delete', [share.week, share],
               method: :delete,
               data: { confirm: 'Are you sure?' } %>
</div>

