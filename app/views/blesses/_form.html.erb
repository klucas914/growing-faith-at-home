<%= form_for([@week, @week.blesses.build]) do |f| %>
  <div class="form-group">
    <%= f.text_area :blessing, class: "form-control" %>
  </div>
  <div class="action">
    <%= f.submit %>
  </div>
<% end %>