<%= form_for([@week, @week.talks.build]) do |f| %>
  <div class="form-group">
    <%= f.label :talking_question %><br>
    <%= f.text_area :question, class: "form-control" %>
  </div>
  <div class="action">
    <%= f.submit %>
  </div>
<% end %>